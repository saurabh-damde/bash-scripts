#!/usr/bin/env bash

# Aliases
alias "cd"="fcd"
alias "edit"="micro $file"
alias "update"="sudo apt update"
alias "upgrade"="sudo apt upgrade"
alias "autoremove"="sudo apt autoremove"
alias "uninstall"="sudo apt remove --purge $app"
alias "install"="sudo apt install $app"
